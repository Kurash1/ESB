base_price = 5.0
terrain_mod = 0
NOT = {
	OR = {
		has_terrain = farmlands
		has_terrain = grasslands
		has_terrain = steppe
		has_terrain = savannah
		has_terrain = marsh
		has_terrain = bayou
		has_terrain = dwemer_stronghold
		has_terrain = lava
	}
}
terrain_mod = 1
always = yes